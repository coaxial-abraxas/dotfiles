language: sh


# From https://github.com/koalaman/shellcheck/wiki/TravisCI
addons:
  apt:
    sources:
      - debian-sid    # Grab shellcheck from the Debian repo (o_O)
    packages:
      - shellcheck

script:
  - shellcheck bootstrap_mac.sh
  - shellcheck lib/scripts/*.sh
  - shellcheck .bash_*

notifications:
  email: false
